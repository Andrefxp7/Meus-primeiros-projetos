function limparInput() {
    document.getElementById("meuInput").value = "";
}

function mostrarAlerta() {
    alert("Sua justificação para o cancelamento dessa reserva foi enviada com sucesso e será analisada por um gerente!");
}